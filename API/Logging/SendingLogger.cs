
namespace OTA.Logging
{
    /// <summary>
    /// Sending logger.
    /// </summary>
    // TODO Determine if this is not extensible
	public enum SendingLogger : int
	{
		PLAYER = 0,
		CONSOLE = 1,
		RCON = 2
	}
}

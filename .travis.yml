language: csharp
solution: OTAPI.sln
install:
  - mozroots --import --sync
  - nuget restore
  - nuget restore OTAPI.Core.Tests/packages.config -PackagesDirectory packages/ -source "https://api.nuget.org/v3/index.json;https://www.nuget.org/api/v2/curated-feeds/microsoftdotnet/"
script:
  - xbuild /p:Configuration=Debug "OTAPI.sln"

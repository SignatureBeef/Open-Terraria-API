language: csharp
solution: OTAPI.sln
install:
  - mozroots --import --sync
  - mono .nuget/NuGet.exe restore API/packages.config -PackagesDirectory packages/ -source "https://api.nuget.org/v3/index.json;https://www.nuget.org/api/v2/curated-feeds/microsoftdotnet/"
  - mono .nuget/NuGet.exe restore Patcher/packages.config -PackagesDirectory packages/ -source "https://api.nuget.org/v3/index.json;https://www.nuget.org/api/v2/curated-feeds/microsoftdotnet/"
script:
  - xbuild /p:Configuration=Debug "OTAPI.sln"
